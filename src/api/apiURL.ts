const baseApiURL: string = "http://localhost:5000/api";


const userURL: string = baseApiURL + "/user";
const userLoginURL: string = baseApiURL + "/user/login";

const userAuthURL: string = baseApiURL + "/user/auth";

const userRegistrationURL: string = baseApiURL + "/user/registration";

const deviceURL: string = baseApiURL + "/device";

const basketURl: string = baseApiURL + "/basket";

const ordersBuyURL: string = baseApiURL+ "/orders"

export {baseApiURL, userURL, userLoginURL, userAuthURL, userRegistrationURL, deviceURL, basketURl, ordersBuyURL};